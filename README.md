# 🛍️ Telegram Web App Shop

Минималистичный интернет-магазин с Telegram Web App интерфейсом и оплатой через Telegram Stars.

## ✨ Особенности

- 📱 Telegram Web App интерфейс
- ⭐ Оплата через Telegram Stars
- 💱 Автоматическая конвертация ГРН → Stars с учетом комиссий платформ
- 🧾 Автоматическая отправка чеков после покупки
- 🎨 Светлая минималистичная тема
- 🔐 Защищенная админ-панель
- 📦 Управление товарами и заказами
- 🗄️ PostgreSQL база данных (для Render)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните переменные окружения:

```env
# Telegram Bot Token (получите у @BotFather)
BOT_TOKEN=your_bot_token_here

# URL вашего приложения (локально или на Render)
WEBAPP_URL=https://your-app.onrender.com

# PostgreSQL Database URL (получите на Render)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password

# Порт сервера
PORT=3000

# Курсы конвертации (Stars за 1 ГРН)
MOBILE_STARS_RATE=1.0    # Мобильные платформы (с комиссией Google Play)
DESKTOP_STARS_RATE=1.2   # Десктоп (без комиссии)
```

### 3. Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте бота командой `/newbot`
3. Следуйте инструкциям и сохраните токен
4. Настройте Web App:
   ```
   /setmenubutton
   Выберите бота
   Введите текст: Открыть магазин
   Введите URL: https://your-app.onrender.com/app
   ```

### 4. Запуск локально

```bash
npm start
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 📋 Структура проекта

```
shop/
├── server.js              # Express сервер
├── database.js            # PostgreSQL база данных
├── bot.js                 # Telegram бот
├── utils/
│   └── currency.js        # Конвертация валют
├── public/
│   ├── admin/            # Админ-панель
│   │   ├── index.html
│   │   ├── dashboard.html
│   │   ├── admin.css
│   │   └── admin.js
│   └── app/              # Telegram Web App
│       ├── index.html
│       ├── style.css
│       └── app.js
└── package.json
```

## 🌐 Деплой на Render

### 1. Создайте Web Service на Render

1. Перейдите на [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите ваш Git репозиторий
4. Настройки:
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Environment**: Node

### 2. Добавьте PostgreSQL

1. Создайте **PostgreSQL** базу данных на Render
2. Скопируйте **Internal Database URL**
3. Добавьте в переменные окружения Web Service

### 3. Настройте переменные окружения

В настройках Web Service добавьте:
- `BOT_TOKEN` - токен вашего бота
- `WEBAPP_URL` - URL вашего Render приложения
- `DATABASE_URL` - URL PostgreSQL базы
- `ADMIN_USERNAME` - логин администратора
- `ADMIN_PASSWORD` - пароль администратора
- `MOBILE_STARS_RATE` - курс для мобильных (1.0)
- `DESKTOP_STARS_RATE` - курс для десктоп (1.2)

### 4. Деплой

Нажмите **Create Web Service** и дождитесь завершения деплоя.

## 🎛️ Использование

### Админ-панель

1. Откройте `https://your-app.onrender.com/admin`
2. Войдите используя данные из `.env`
3. Добавьте товары:
   - Название
   - Описание
   - Цена в ГРН
   - Изображение (опционально)
4. Просматривайте заказы в разделе "Заказы"

### Пользовательский интерфейс

1. Откройте бота в Telegram
2. Нажмите `/start` или кнопку меню
3. Выберите товары и добавьте в корзину
4. Нажмите "Оформить заказ"
5. Оплатите через Telegram Stars
6. Получите чек в боте

## 💱 Конвертация валют

Система автоматически определяет платформу пользователя и применяет соответствующий курс:

- **Мобильные** (Android, iOS): ~1.0 Stars/грн (учет комиссии Google Play ~30%)
- **Десктоп** (Windows, macOS, Web): ~1.2 Stars/грн (без комиссии)

Курсы можно настроить в `.env`:
```env
MOBILE_STARS_RATE=1.0
DESKTOP_STARS_RATE=1.2
```

## 🧾 Чеки

После успешной оплаты пользователь автоматически получает чек в боте с информацией:
- Список товаров и количество
- Сумма в ГРН и Stars
- Дата и время
- ID платежа

## 🔒 Безопасность

- Админ-панель защищена паролем (bcrypt хеширование)
- Все платежи обрабатываются через Telegram API
- PostgreSQL база данных с SSL в production
- Валидация данных на backend

## 📝 API Endpoints

### Products
- `GET /api/products` - получить все товары
- `GET /api/products/:id` - получить товар
- `POST /api/products` - создать товар (admin)
- `PUT /api/products/:id` - обновить товар (admin)
- `DELETE /api/products/:id` - удалить товар (admin)

### Orders
- `POST /api/orders` - создать заказ
- `GET /api/orders` - получить все заказы (admin)

### Admin
- `POST /api/admin/login` - вход в админ-панель

## 🛠️ Технологии

- **Backend**: Node.js, Express
- **Database**: PostgreSQL
- **Bot**: node-telegram-bot-api
- **Frontend**: Vanilla JavaScript, HTML, CSS
- **Payments**: Telegram Stars (XTR)
- **Deployment**: Render

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте переменные окружения
2. Убедитесь, что бот настроен правильно
3. Проверьте логи на Render
4. Убедитесь, что PostgreSQL база подключена

## 📄 Лицензия

MIT
